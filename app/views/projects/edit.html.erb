
<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <p class="lead"><%= render 'form', project: @project %></p>
    <p align="center">
        <a class="btn btn-primary btn-sm" <%= link_to 'Show', @project %></a>
        <a class="btn btn-primary btn-sm" <%= link_to 'Back', projects_path %></a>
    </p>
  </div>
</div>